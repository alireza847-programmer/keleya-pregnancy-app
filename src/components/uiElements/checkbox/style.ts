import {StyleSheet} from 'react-native';
import {radius} from 'themes/global';
import {wp} from 'utils/dimension';

export default StyleSheet.create({
  conteiner: {
    width: wp(7),
  },
  iconStyle: {
    borderRadius: radius,
  },
});
